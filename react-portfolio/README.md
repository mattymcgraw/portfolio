# React Portfolio Sample

This directory contains a React-based version of the portfolio using Vite.

## Run locally

1. cd react-portfolio
2. npm install
3. npm run dev

Then open the local URL shown by Vite.
